# OpenWeather API key — used to fetch hourly forecasted weather data that
# enriches AQI analysis (temperature, humidity, wind, conditions).
# Register a free key at https://openweathermap.org/api
VITE_OPENWEATHER_API_KEY=your_openweather_api_key_here